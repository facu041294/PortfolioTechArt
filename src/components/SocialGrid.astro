---
// src/components/SocialGrid.astro
import socialData from '../data/social.json';
---

<div class="connect-buttons-grid">
  {socialData.map(({ platform, url, svg }) => (
    <a 
      href={url} 
      target="_blank" 
      rel="noopener noreferrer" 
      class="connect-button"
      aria-label={`Visita mi perfil de ${platform}`}
    >
      <span class="connect-button-icon" aria-hidden="true" set:html={svg} />
      {platform}
    </a>
  ))}

  {/* Botón label disparador para el formulario de contacto */}
  <label 
    for="contact-toggle-checkbox" 
    class="connect-button contact-toggle-button-styled" 
    role="button" 
    tabindex="0" 
    aria-expanded="false"
  >
    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24"
      viewBox="0 0 24 24" fill="none" stroke="currentColor"
      stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
      class="lucide lucide-send-icon lucide-send">
      <path d="M14.536 21.686a.5.5 0 0 0 .937-.024l6.5-19a.496.496 0 0 0-.635-.635l-19 6.5a.5.5 0 0 0-.024.937l7.93 3.18a2 2 0 0 1 1.112 1.11z"/>
      <path d="m21.854 2.147-10.94 10.939"/>
    </svg>
    Mail Directo
  </label>
</div>
